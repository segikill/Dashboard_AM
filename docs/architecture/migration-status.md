# Состояние миграции React + TypeScript

Дата проверки: 19.08.2026.

## Резервная точка

- Каталог: `C:\Users\User\Documents\Смертность\backups\Dashboard_AM_pre_react_20260818_194934`.
- Исходный commit: `b7d7e58b470096fd210657f0ef46eb2fa99363d3`.
- ZIP рабочей версии и полный Git bundle проверены контрольными суммами SHA-256.

## Этап 1 — завершён

- Добавлены Node.js 24, Vite, React 19, TypeScript и Vitest.
- Production-модуль собирается в `assets/react/atlas-hybrid.js`.
- React загружается после основного интерфейса и не блокирует первоначальную отрисовку.
- Между legacy-ядром и React действует типизированный событийный bridge.
- Исходные данные и расчётные функции на этом этапе не переносились.

## Этап 2.1 — завершён

- Глобальные фильтры периода, пола и возраста перенесены в React/TypeScript.
- Состояние остаётся единым: React записывает значения в существующее ядро, а legacy-вкладки публикуют изменения обратно в React.
- При ошибке загрузки React старые фильтры автоматически остаются видимыми и рабочими.
- На вкладке инфраструктуры сохранена пустая полоса без фильтров и без сдвига рабочей области.
- Значения фильтров сохраняются при переключении вкладок.
- Колесо мыши продолжает переключать значения списков через общий обработчик атласа.

## Этап 2.2 — завершён

- Верхняя строка состояния перенесена в React/TypeScript: период, готовность данных и число наблюдений в текущем фильтре.
- Кнопки «Ссылка», SVG, CSV и справка перенесены в React, но вызывают проверенные legacy-обработчики экспорта без дублирования расчётной логики.
- Доступность SVG синхронизируется с активной вкладкой; на карте инфраструктуры действие корректно отключено.
- React и legacy используют единый типизированный контракт состояния. Если React не загрузится, прежняя верхняя строка остаётся рабочим fallback.
- В контрактную проверку добавлен контроль счётчика: значение не может быть отрицательным или превышать общий объём данных.

## Этап 3.1 — завершён

- Верхняя панель карты смертности перенесена в React: управление левой панелью параметров, возврат к охвату всего региона и показ правой аналитической панели.
- React формирует заголовок правого аналитического контекста и отражает название закреплённой территории; легенда, рейтинг и карточка объекта продолжают использовать проверенные расчёты legacy-ядра.
- MapLibre, WebGL-canvas, источники и слои не пересоздаются при открытии и закрытии панелей. Это контролируется production browser smoke сравнением ссылки на canvas до и после действий.
- На ширинах до 1540 px правая панель остаётся выдвижной, на широких экранах её можно освободить вручную. После изменения компоновки выполняется только `map.resize()`.
- При недоступном React автоматически сохраняется прежняя панель карты и её кнопки.

## Этап 3.2 — завершён

- Заголовок карты медицинской инфраструктуры, сворачивание панели состава карты и возврат к охвату всей области перенесены в React/TypeScript.
- MapLibre, WebGL-canvas, 417 медицинских объектов, кластеризация, изохроны, дороги, границы и населённые пункты остались в существующем картографическом ядре.
- Открытие и закрытие панели не пересоздаёт карту: browser smoke сравнивает один и тот же canvas до и после действий React и вызывает только `map.resize()`.
- Внешняя кнопка панели и новая React-кнопка используют единое состояние через типизированный bridge. При недоступном React автоматически остаются видимыми и рабочими прежние заголовок и кнопки.

## Этап 4.1 — завершён

- Левая рейка с восемью визуализациями и общей кнопкой боковой панели перенесена в React/TypeScript.
- Переключение вкладок использует типизированный bridge и существующий кэш представлений: фильтры сохраняются, а MapLibre-canvas карты смертности остаётся тем же после полного цикла переходов.
- Добавлена клавиатурная навигация по рейке: `ArrowUp`, `ArrowDown`, `Home` и `End`; активная вкладка публикуется через `aria-current` и `aria-pressed`.
- Исходная HTML-рейка сохранена как fallback и автоматически скрывается только после успешного монтирования React.
- Отложенная загрузка React отвязана от `window.load` и запускается после `DOMContentLoaded` в свободное время браузера. Медленные сторонние тайлы больше не задерживают появление нового интерфейса.

## Этап 4.2 — завершён

- Панель настроек Treemap, хлебные крошки и правый аналитический контекст перенесены в React/TypeScript.
- Расчёт агрегатов, иерархия МКБ-10 и раскладка прямоугольников оставлены в проверенном JavaScript-ядре; React получает только компактный типизированный снимок выбранной категории и команды управления.
- Сохранена согласованная логика взаимодействия: первый клик выбирает плитку и обновляет правую панель, повторный быстрый клик открывает следующий уровень; явная кнопка перехода и хлебные крошки работают как альтернативная навигация.
- React управляет количеством/долей/ПГПЖ-75/коэффициентом, цветовой метрикой, сортировкой, подписями и порогом группы «Прочие». Шкала объёма читается от тёмных малых значений к более ярким крупным.
- Старые панель, хлебные крошки и инспектор сохранены в DOM как fallback и скрываются только после успешного получения React-владения соответствующим контейнером.
- Публичный мост не передаёт React исходные записи: выбранный элемент представлен обезличенной сводкой, а дочерний список ограничен шестью строками.

## Этап 4.3 — завершён

- Настройки строк и показателя матрицы «территория × причина», а также правый аналитический контекст выбранной ячейки перенесены в React/TypeScript.
- Матрица получила устойчивый порядок муниципальных территорий, выбор ячейки с клавиатуры и мышью, закреплённое выделение, ранги территории и причины, формулу коэффициента и топ территорий по выбранному классу МКБ-10.
- Для муниципального уровня всегда выводятся все 29 территорий; для населённых пунктов доступны топ-25, топ-50 и полный перечень. Переключение выполняется через типизированный bridge без передачи React исходных строк смертности.
- Расчёт агрегатов и коэффициентов остался в проверенном JavaScript-ядре. Для доступа к ячейкам используется индекс `Map`, поэтому выбор и обновление аналитической панели не выполняют повторный линейный поиск по всей матрице.
- Legacy-настройки и инспектор сохранены как fallback и скрываются только после успешного монтирования соответствующих React-islands.
- Отдельно проверены выбор показателя, переход муниципалитеты ↔ населённые пункты, лимиты строк, выбор ячейки и адаптивная компоновка на 1366, 1920 и 2560 px.

## Этап 4.4 — завершён

- Настройки Arrow diagram и правая аналитическая панель перенесены в React/TypeScript. В интерфейсе явно разделены режимы «Сравнение» и «Динамика», а также доступны срезы по времени и полу, уровни МКБ-10, показатель, глубина списка и фильтр изменившихся позиций.
- Расчёт рангов, подготовка рядов и SVG-отрисовка оставлены в проверенном JavaScript-ядре. React получает компактную типизированную сводку активного режима, выбранной причины и шести наиболее заметных перемещений; исходные записи смертности через мост не передаются.
- Правая панель показывает текущий показатель, направление и величину изменения ранга, значения до и после, изменение доли, медианный возраст, ПГПЖ-75 и автоматически сформированный вывод. Выбор строки в панели синхронизирован с выделением траектории на SVG.
- Legacy-настройки и прежний аналитический контекст сохранены как рабочий fallback и скрываются только после успешного монтирования React-islands.
- Browser smoke проверяет оба режима, временной и половой срезы, уровни классов и блоков, показатели количества и доли, глубину списка, выбор причины и адаптивную компоновку на 1366, 1920 и 2560 px.

## Этап 4.5 — завершён

- Настройки возрастно-половой пирамиды и правая аналитическая панель перенесены в React/TypeScript. Доступны режимы структуры, динамики и различий между мужчинами и женщинами, уровни классов/блоков/кодов МКБ-10, причина, показатель, шаг возрастных групп и плотность подписей.
- Возрастные агрегаты и SVG-отрисовка оставлены в проверенном JavaScript-ядре. React получает только компактную типизированную сводку: общие итоги, выбранную возрастную группу и пять ведущих возрастных групп; исходные строки смертности через мост не передаются.
- Правая панель показывает численность и доли мужчин и женщин, их соотношение, ведущий класс МКБ-10, ПГПЖ-75, изменение к базовому периоду и короткий автоматически сформированный вывод. Выбор строки в панели синхронизирован с возрастной группой на SVG.
- Сохранена ранее утверждённая логика графика: постоянного активного диапазона и приглушения остальных возрастных групп нет. Клик обновляет сведения справа, не превращая строку в закреплённый визуальный фильтр.
- Глобальные фильтры пола и возраста на этой вкладке отключены, потому что они являются осями самой пирамиды. Фильтр года доступен в структуре и различиях полов и автоматически отключается только в режиме многолетней динамики.
- Legacy-настройки и прежний аналитический контекст сохранены как рабочий fallback и скрываются только после успешного монтирования React-islands.
- Browser smoke проверяет все три режима, уровни МКБ-10, количество/долю/ПГПЖ-75, шаг 5/10 лет, плотность подписей, выбор возрастной группы и адаптивную компоновку на 1366, 1920 и 2560 px.

## Этап 4.6 — завершён

- Настройки вкладки «Возраст смерти по причинам» и правая аналитическая панель перенесены в React/TypeScript. Интерфейс объединяет режимы «Профиль», «Сравнение» и «Одна причина» без скрытых управляющих вкладок внутри графика.
- React управляет уровнями классов/блоков/кодов МКБ-10, родительским классом, порогом надёжности, числом строк, видом внешнего интервала, сортировкой, сравнением лет или полов и шкалой распределения.
- Расчёт медианы, Q1–Q3, P10–P90, полного диапазона, ПГПЖ-75, доли до 75 лет, плотности и пятилетней гистограммы оставлен в оптимизированном JavaScript-ядре. Группы причин по-прежнему агрегируются за один проход по отфильтрованным данным.
- React получает только компактный типизированный снимок текущего контекста и пяти ведущих строк. Список причин состоит из справочных подписей МКБ-10 и кэшируется по уровню/классу; исходные строки смертности через мост не передаются.
- Правая панель показывает выбранную причину, основной показатель, изменение, интервалы, ПГПЖ-75, место в списке, ведущую территорию и автоматически сформированный вывод. Из панели можно открыть полное распределение выбранной причины.
- Сохранена единая логика глобальных фильтров: при сравнении лет временно отключается верхний период, при сравнении полов — верхний фильтр пола; в остальных режимах они доступны.
- Legacy-настройки, внутренние вкладки и прежний инспектор остаются рабочим fallback и скрываются только после успешного получения React-владения.
- Основной browser smoke и отдельный тест вкладки проверяют три режима, выбор причины, распределение, уровни МКБ-10, фильтры, длинные подписи, отсутствие внутренней прокрутки и компоновку 1366/1920/2560 px.

## Этап 4.7 — завершён

- Настройки Dotogram территорий и правая аналитическая панель перенесены в React/TypeScript. Доступны уровни населённых пунктов и муниципалитетов, все классы МКБ-10, семь показателей и три режима подписей НП.
- Сохранены две ранее разработанные формы графика: НП сгруппированы по муниципальным территориям, а муниципалитеты представлены ранжированным точечным профилем. Пространственное разведение, оси и SVG-отрисовка остаются в проверенном JavaScript-ядре.
- Каждая SVG-точка получила устойчивый ключ, клавиатурный выбор, закреплённое выделение и синхронизацию с React-панелью. Выбирать территорию можно как непосредственно на графике, так и в списке ведущих значений.
- Правая панель показывает показатель, место территории, медиану распределения, число смертей, выбранную причину, долю внутри территории, население 2021 года и автоматически сформированный вывод о положении относительно медианы и статистических выбросах.
- Публичный мост передаёт React только компактные агрегаты выбранной территории и десяти ведущих строк; массивы возрастов и исходные записи смертности в контракт не включаются.
- Устранена двойная SVG-отрисовка. Для повторных переключений НП ↔ МО добавлен ограниченный LRU-кэш на шесть состояний; извлечённое состояние удаляется из кэша, поэтому оно не конфликтует с общим кэшем вкладок.
- Legacy-настройки и прежний аналитический контекст сохранены как рабочий fallback и скрываются только после успешного получения React-владения.
- Browser smoke проверяет 503 НП с наблюдениями в текущем агрегате, все 29 муниципальных территорий, выбор точки, коэффициент на 100 тысяч, класс МКБ-10 и компоновку 1366/1920/2560 px.

## Контроль данных

| Набор | Контрольное значение |
|---|---:|
| Наблюдения | 30 232 |
| Классы МКБ-10 | 22 |
| Крупные блоки | 133 |
| Трёхзначные коды | 577 |
| Муниципальные территории | 29 |
| Населённые пункты | 632 |

До этапа 5.1 файлы `data/`, `tables/` и `manifest.json` не изменялись. На этапе 5.1 в `data/` добавлен отдельный слой данных атласа; существующие наборы и таблицы не пересчитывались.

## Проверки этапов 2.1–4.7

- `npm run build`: успешно, 26 модулей.
- `npm test`: 13 из 13 тестов успешно.
- Production browser smoke: успешно, ошибок консоли и JavaScript нет.
- Проверены вкладки: карты смертности и инфраструктуры, treemap, матрица, ранги, пирамида, возраст и точки.
- Для Treemap отдельно проверены выбор плитки, повторный переход внутрь, возврат по хлебным крошкам, синхронизация всех настроек и переключение React/fallback.
- Для Heatmap отдельно проверены React/fallback, показатель, уровень территорий, лимит населённых пунктов, выбор ячейки и обновление правой аналитической панели.
- Для Arrow diagram отдельно проверены React/fallback, режимы сравнения и динамики, временной и половой срезы, уровни МКБ-10, показатели, глубина списка, выбор траектории и обновление правой панели.
- Для возрастно-половой пирамиды отдельно проверены React/fallback, три режима, уровни МКБ-10, показатели, шаг возрастных групп, подписи, выбор строки и согласованность глобальных фильтров.
- Для возраста смерти по причинам отдельно проверены React/fallback, профиль, сравнение, распределение одной причины, все параметры выборки, выбор строки и синхронизация глобальных фильтров.
- Для Dotogram отдельно проверены React/fallback, НП и МО, количество, коэффициент, класс МКБ-10, подписи, выбор точки с SVG и из правой панели, а также повторное восстановление вкладки из кэша.
- Проверены ширины 1366, 1920 и 2560 px: горизонтального переполнения нет, панели обеих карт и шести перенесённых аналитических вкладок остаются внутри своих областей.
- Production bundle: 346,17 КБ, gzip 83,41 КБ; загружается лениво.

## Производительность production-сборки

Контрольный прогон Chrome 1920 × 1080:

- критическая передача до отложенной загрузки React: 1 740 926 байт;
- общая передача после загрузки отложенного React-модуля: 2 087 403 байта;
- DOMContentLoaded: 1 569 мс в сопоставимом контрольном прогоне;
- переключение на Treemap: медиана 68,0 мс;
- переключение на Heatmap: медиана 75,1 мс;
- переключение на Arrow diagram: медиана 99,7 мс;
- переключение на возрастно-половую пирамиду: медиана 47,8 мс;
- переключение на «Возраст смерти по причинам»: медиана 20,8 мс;
- переключение на Dotogram: медиана 17,8 мс;
- переключение НП ↔ МО: медиана 51,8 мс после оптимизации, до оптимизации — 410,2 мс;
- смена показателя Dotogram: медиана 58,0 мс; выбор территории — 26,9 мс;
- изменение глобального фильтра: 56,2 мс;
- память после набора переходов и заполнения ограниченного SVG-кэша: 36,4 МБ;
- ошибок: 0.

Относительно этапа 4.6 критическая передача выросла на 18 070 байт, а полный объём передачи — на 26 633 байта. Production-бандл вырос на 8,56 КБ, gzip — на 1,16 КБ. Полный объём React отражён отдельно от критического пути. Медианы переключения всех шести перенесённых аналитических вкладок и действий Dotogram остаются ниже целевого порога 200 мс; функциональных и консольных ошибок нет.

## Этап 5.1 — завершён

- Объект `DATA` на 30 232 обезличенных наблюдения вынесен из `index.html` в `data/atlas-data.js`. Legacy-ядро по-прежнему получает тот же объект через `const DATA=window.AMUR_ATLAS_DATA`, поэтому расчётные функции не менялись.
- `index.html` уменьшен с 1 202 933 до 67 625 байт. Набор подключается как отдельный ресурс с 12-значной версией из SHA-256 в URL.
- Добавлена JSON Schema `data/atlas-data.schema.json` и проверка `npm run data:validate`: контроль SHA-256, корневых полей, длин массивов, шестипольной структуры записи, ссылочных индексов и итогов качества.
- Валидатор отдельно подтверждает, что коды Y35–Y36 не используются ни в одной активной записи.
- Скрипт `scripts/extract_atlas_data.cjs` воспроизводит миграцию и безопасен при повторном запуске. Сборщик инфраструктурной карты научен читать как старый встроенный, так и новый внешний формат.
- Проверка данных включена в `npm test` и `npm run build`, чтобы повреждённый или неполный набор не попал в production-сборку.
- `npm test`: 13 из 13 тестов; `npm run build`: 26 модулей, React-бандл 346,17 КБ (gzip 83,41 КБ). Production browser smoke прошёл все восемь вкладок и компоновки 1366/1920/2560 px без JavaScript- и консольных ошибок.
- Прямое A/B-сравнение в одном локальном Chrome 1920 × 1080: HTML-ответ уменьшился с 1 203 233 до 67 925 переданных байт; суммарная передача первой загрузки практически не изменилась (3 290 602 → 3 291 308 байт), потому что те же данные теперь идут отдельным ресурсом. DOMContentLoaded в контрольном прогоне сократился с 1 646 до 1 371 мс; это локальный ориентир, а не production-SLA.

## Следующий шаг

Этап 5.2: разделить тяжёлые справочники и геометрию на отдельные кэшируемые ресурсы и загружать их по требованию без потери fallback-режима.
